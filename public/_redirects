# Proxy API to Render (must be BEFORE SPA catch-all; ! = force)
/api/*  https://yureka-one.onrender.com/api/:splat  200!

# Allow search engine verification files
/google432b4c0815ba33ee.html /google432b4c0815ba33ee.html 200

# React SPA catch-all (netlify.toml also defines this; keep force here so /api wins)
/* /index.html 200
