services:
  - type: web
    name: yureka-api
    env: node
    buildCommand: npm install && npm run build && pip install -r requirements.txt
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PYTHON_VERSION
        value: 3.11.2
