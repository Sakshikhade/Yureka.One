services:
  - type: web
    name: yureka-api
    env: node
    buildCommand: npm install && npm run build && pip3 install -r requirements.txt --break-system-packages
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
