services:
  app:
    image: ${IMAGE:-ghcr.io/toanweshbiswas/yureka.one:latest}
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
